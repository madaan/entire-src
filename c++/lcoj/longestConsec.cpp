//sg
#include<bits/stdc++.h>
using namespace std;
int longestConsecutive(vector<int> &num) {
    unordered_map<int, int> pm;
    for(auto &x:num) {
        pm[num] = 1;
    }
    for(auto kv : pm) {
        int num = kv.first;

