
//sg
#include <stdio.h>
#include <algorithm>
#define max(a, b) (a > b ? a : b)
#define MAX 100010
int main(int argc, char *argv[])
{
    for(int i = 0; i < 10000; i++) {
        printf("%s", argv[1]);
    }

return 0;
}
